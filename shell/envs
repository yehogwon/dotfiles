export LC_ALL=en_US.UTF-8
export TMUX_POWERLINE_CONFIG_FILE="$(cd -- "$(dirname -- "${BASH_SOURCE[0]:-$0}")" && pwd -P)/../tmux/powerline/config.sh"

[ -d "$HOME/.local/bin" ] && export PATH="$HOME/.local/bin${PATH:+:$PATH}"

